# Copy to `.env` (gitignored — persists across git branch switches on this machine).
# Run: dart run tool/bootstrap_env.dart

# Backend (optional — defaults to production if unset)
API_BASE_URL=https://portal.capitallocums.co.uk/api

# Restricted client key for Places Autocomplete / Maps (never commit real values)
GOOGLE_MAPS_API_KEY=


